<%= form_for get_current_user.active_relationships.build(followed_id: @user.id) ,remote: true do |f|%>
  <%= f.submit "Follow",class: "btn form-control btn-primary"%>
  <%= hidden_field_tag :id,@user.id %>
<%end %>