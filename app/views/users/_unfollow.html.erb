<%= form_for get_current_user.active_relationships.find_by(followed_id: @user.id),html: {method: :delete},
    remote: true do |f|%>
  <%= f.submit "Unfollow",class: "btn form-control btn-danger"%>
<%end %>