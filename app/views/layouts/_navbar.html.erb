<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container no-margin">
    <div class="logo">
      <%= link_to image_tag("twitter.png",width:'64px',height: '32px'),"http://www.twitter.com" %>
    </div>
    <div class="pull-right">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <%= home_page_view(yield(:title))%> <%= "active" if yield(:title) == "Home" %>">
            <%=link_to "Home" , "/static_pages/home" ,class: "nav-link"%>
          </li>
          <li class="nav-item  <%= help_page_view(yield(:title))%> <%= "active" if yield(:title) == "Help" %>">
            <%=link_to "Help" , "/static_pages/help" ,class: "nav-link"%>
          </li>
          <li class="nav-item <%= about_page_view(yield(:title))%> <%= "active" if yield(:title) == "About" %>">
            <%=link_to "About" , "/static_pages/about",class: "nav-link" %>
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0 <%= search_form_view(yield(:title))%>">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-primary my-2 my-sm-0" type="submit">Search</button>
        </form>
      </div>
    </div>
  </div>
</nav>